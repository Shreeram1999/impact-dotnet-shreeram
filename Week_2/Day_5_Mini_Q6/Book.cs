// Mini Project Q6 - Library Management. Just a plain data record; all the
// interesting LINQ logic lives in LibraryQueries.cs.
public record Book(string Title, string Author, string Genre, int Year, bool IsAvailable);
